export async function fetchCompareData() {
  // 🔹 ตอนนี้ mock ไว้ก่อน
  // 🔹 เปลี่ยนเป็น fetch("YOUR_API_URL") ได้ทันที

  return {
    target: [],
    compare1: [],
    compare2: [],
    compare3: [],
  };
}
